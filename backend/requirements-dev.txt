# ═══════════════════════════════════════════════════════
# NIX AI Backend — Dev / Test Dependencies
#
# Install: pip install -r requirements-dev.txt
# (includes all production deps + testing tools)
# ═══════════════════════════════════════════════════════

-r requirements.txt

# ── Testing ──
pytest==8.2.0
pytest-asyncio==0.23.0
moto[dynamodb,s3,sqs]==5.0.0

# ── Dev Tools ──
httpx==0.27.0
